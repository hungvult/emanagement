# Client-side Environment Variables (Available in Browser)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_APP_NAME="eManagement System"

# Server-side Environment Variables (Only available in Next.js Server Runtime)
PORT=3000
HOSTNAME=0.0.0.0
